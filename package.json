{
  "name": "kikoeru",
  "description": "A self-hosted media server for listening to your DLsite voice works.",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "start:backend": "npm run -w backend start",
    "start:bundle": "npm run -w backend start:bundle",
    "dev": "npm run -w backend dev",
    "scan": "npm run -w backend scan",
    "update": "npm run -w backend update",
    "build:backend": "npm run -w backend build",
    "build:frontend": "npm run -w frontend build",
    "build:all": "npm run build:frontend && npm run build:backend",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "typecheck": "tsc --noEmit",
    "test:unit": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test": "npm run lint && npm run typecheck && npm run test:unit"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@vitest/coverage-v8": "^4.1.11",
    "esbuild": "^0.28.2",
    "eslint": "^10.9.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-n": "^18.3.0",
    "globals": "^17.11.0",
    "prettier": "^3.9.6",
    "typescript": "^7.0.2",
    "typescript-eslint": "^8.68.0",
    "vitest": "^4.1.8"
  }
}
